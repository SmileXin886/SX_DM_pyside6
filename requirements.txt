# Dreamina Toolkit - Python 中转端依赖
# Python 3.9+ 推荐

# Web 服务与 WebSocket 通信
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
websockets>=12.0

# 媒体处理 - 视频预览和缩略图生成
opencv-python>=4.8.0
ffmpeg-python>=0.2.0

# 浏览器控制 (CDP 接管)
playwright>=1.41.0

# HTTP 客户端 (用于可能的外部 API 调用)
httpx>=0.26.0
